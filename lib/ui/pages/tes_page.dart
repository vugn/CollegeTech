import 'package:flutter/material.dart';

class TESWIDGET extends StatelessWidget {
  const TESWIDGET({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Placeholder(),
    );
  }
}
